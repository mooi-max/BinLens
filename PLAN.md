# GTFOBins 离线速查（非官方）— 完整开发计划

## 1. 产品目标

开发一个 Windows 10/11 x64 的绿色单文件 EXE：

- 双击即可使用，无需安装、无需运行环境；
- 内置 GTFOBins 官方数据，可完全断网检索；
- 界面、说明和功能标签支持中文/English 切换；
- GTFOBins 的命令、二进制名、路径、参数始终保持官方原文；
- 不执行、不生成、不上传任何命令或靶机信息；
- 首版专注：单条检索、批量分析、查看详情、复制命令；
- 在线更新只更新完整 EXE，不提供独立数据包更新。

应用名称：**GTFOBins 离线速查（非官方）**。

## 2. 技术方案

- 技术栈：`.NET 8 + WPF`
- 发布形式：`win-x64`、self-contained、single-file EXE
- 数据格式：嵌入 EXE 的压缩 JSON
- 本地设置：仅保存主题、语言和窗口状态；不保存搜索内容或批量粘贴内容
- 网络行为：仅在用户点击“检查更新”后访问 GitHub Releases
- 无账号、无遥测、无广告、无浏览器内嵌组件

程序启动后不需要网络，也不请求任何外部服务。

## 3. 数据与双语策略

每个 GTFOBins 条目保存：

- 规范命令名与官方别名；
- 原始英文说明；
- 对应中文翻译；
- 功能场景：Sudo、SUID、Capabilities、Shell、File Read/Write、Reverse Shell 等；
- 每个场景下的官方命令块及说明；
- 官方来源文件路径、上游 Git commit、数据版本。

处理规则：

- 命令文本逐字符保留，不翻译、不重写、不补全；
- 中文只翻译说明、标题、分类和界面文案；
- English 切换显示官方英文内容；
- 条目数据同步时保留上游 GPL-3.0 声明、版权信息和来源；
- 每次发布在“关于”页显示内置数据的上游版本，但不显示安全免责声明。

## 4. UI 与交互设计

整体采用简约、专业、低干扰的桌面工具风格：

- 深蓝/石墨灰为主视觉；
- 默认跟随系统浅色/深色主题，可在右上角手动切换；
- 默认中文，可切换 English；
- 不使用复杂侧边栏、弹窗流程或多层页面；
- 以 1366×768 屏幕为最低主要适配尺寸。

主界面结构：

- 顶部：应用名称、语言切换、主题切换、检查更新；
- 中部：大搜索框与“批量分析”入口；
- 下部左侧：检索结果列表；
- 下部右侧：条目详情、场景标签、命令代码框和来源信息。

命令复制规则：

- 单击命令代码框任意位置：复制完整命令；
- 鼠标拖动选择：仅选择文字，不自动复制；
- 按 `Ctrl+C`：复制选中内容；
- 说明文字、批量结果、原始授权行均支持拖选和 `Ctrl+C`；
- 成功复制后显示短暂“已复制”提示，不弹出打断性对话框。

快捷键：

- `Ctrl+F`：聚焦主搜索框；
- `Ctrl+V`：在批量分析页面粘贴剪贴板文本。

Logo 和应用图标：

- 使用原创的终端提示符、命令块与抽象括号元素；
- 深蓝/石墨灰风格；
- 不使用或模仿 GTFOBins 官方视觉标识。

## 5. 单条检索

支持按以下内容即时搜索：

- 二进制名：`find`、`vim`、`python`；
- 版本或别名：`python3`；
- 功能场景：Sudo、SUID、Capabilities 等；
- 中文/英文说明关键词。

结果按匹配质量标识：

- 精确匹配；
- 官方别名匹配；
- 名称相近，需人工确认；
- 未收录。

不把近似名称自动当作同一二进制，也不擅自将 `python3` 命令改写成 `python`；关联官方条目时会明确显示匹配依据。

## 6. 批量分析：`sudo -l` 首版重点

用户将 `sudo -l` 原始输出粘贴到批量分析页后，程序仅在本机解析。

处理流程：

1. 识别 sudo 授权规则中的可执行文件，而不是全局抓取所有 `/path`。
2. 排除 `secure_path` 等 Defaults 配置中的路径，避免误识别。
3. 提取每条授权规则的：
   - 原始授权行；
   - RunAs 条件；
   - `NOPASSWD` 等标签；
   - 命令绝对路径；
   - 参数限制、通配符和禁止规则；
   - 是否带 `!` 禁止项。
4. 从路径中提取命令名，再进行精确或官方别名匹配。
5. 自动判定当前输入来自 `sudo -l`，默认只显示每个条目的 **Sudo** 场景。
6. 点击结果后再展开具体的官方 Sudo 命令，保持结果页简洁。

结果分为：

- 已精确匹配；
- 官方别名匹配；
- 待人工确认；
- 未收录；
- 被明确禁止的规则。

每项都保留原始授权行，展示“为何匹配”的依据；程序不推断命令一定可利用，也不隐藏未匹配项。

解析器不能依赖英文标题文本，应兼容不同语言环境的 `sudo -l` 输出，主要依据授权规则的结构、路径、RunAs 表达式和标签解析。

后续扩展顺序：

1. `sudo -l`
2. SUID 文件列表
3. `getcap` / Linux Capabilities 输出
4. 可写 PATH、服务配置等离线可解析信息

## 7. 更新机制

更新仅针对完整 EXE：

1. 用户点击“检查更新”。
2. 程序访问自己 GitHub 仓库的 Releases 最新版本。
3. 显示版本号、更新摘要、文件大小。
4. 用户确认后下载新版 EXE。
5. 校验发布的 SHA-256。
6. 当前程序退出后，临时更新进程替换旧 EXE 并自动重启。
7. 下载、校验或替换失败时保留旧版本，不影响继续使用。

默认不自动检查更新；后续可在设置中增加“启动时检查更新”选项。

每个新 EXE 都内置同步后的 GTFOBins 数据，因此应用更新即数据更新。

## 8. GitHub、开源与发布

仓库采用 GPL-3.0：

- 保留 GTFOBins 的 LICENSE、版权和来源说明；
- 本项目源码、构建脚本、翻译数据和数据转换脚本公开；
- README 明确标注“基于 GTFOBins 二次开发，非官方客户端”；
- GitHub README 放置用户要求的免责声明；
- Release 同时提供 EXE、SHA-256 校验文件、版本说明和数据版本号。

GitHub Actions 流程：

- 拉取固定版本的上游 GTFOBins 数据；
- 生成/校验双语离线 JSON；
- 验证命令块未被改写；
- 运行解析器和 UI 测试；
- 构建单文件 Windows EXE；
- 生成 SHA-256；
- 发布 GitHub Release。

上游数据同步先生成可审查的 Pull Request，不直接自动发布，避免翻译或数据转换错误进入正式版本。

## 9. 测试与验收标准

数据测试：

- 所有条目可读取；
- 命令内容与上游一致；
- 英中切换不改变命令内容；
- 数据版本、来源文件和上游 commit 完整。

批量解析测试：

- 英文、中文及不同格式的 `sudo -l` 样本；
- `NOPASSWD`、RunAs、绝对路径、参数、逗号分隔、多行规则；
- `!` 禁止项；
- `secure_path` 不得误识别为可执行命令；
- `find`、`python3`、`kill` 等精确、别名和未收录情况。

UI 测试：

- `Ctrl+F`、`Ctrl+V`、鼠标点击复制、拖选后 `Ctrl+C`；
- 中文/English 和浅色/深色切换；
- 1366×768 下无关键内容遮挡；
- 断网启动、检索和批量分析均正常；
- 更新失败、校验失败时旧 EXE 保持可用。

交付验收：

- Windows 10/11 x64 双击 EXE 可启动；
- 不要求安装 .NET、Node.js 或 Python；
- 首次启动和断网使用均可完成全部首版功能；
- 不存在后台网络请求、账号功能或遥测功能。
